version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:20.0

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: npm install

      - run:
          name: Run tests
          command: npm test -- --watchAll=false

      - run:
          name: Build
          command: npm run build

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
